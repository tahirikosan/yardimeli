<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tahirabuzetoglu.yardimeli">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yardimeli">
        <activity android:name=".ui.postdetail.PostDetailActivity"></activity>
        <activity android:name=".ui.edit_profile.EditProfileActivity"></activity>
        <activity android:name=".ui.liked.LikedPostsActivity"></activity>
        <activity android:name=".ui.other_user.OtherUserProfileActivity"></activity>
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity
            android:name=".ui.comment.CommentActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".ui.main.newpost.NewPostActivity" android:windowSoftInputMode="adjustPan" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.main.FeedActivity" />
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>